const Loader = () => {
    return (
        <div className="fixed inset-0 flex items-center w-full h-full justify-center bg-opacity-50 bg-gray-500">
            <div className="loader"></div>
        </div>
  )
}

export default Loader